<section class="comments card">
  <div class="card-header">Comments</div>
  {{#if this}}
    {{#each this}}
      <div class="comment">
        <p>{{commentEl}}</p>
        <p>&mdash; {{user.username}} on {{format_date date_created}}</p>
      </div>
    {{/each}}
  {{else}}
    <div class="comment">
      <p>There ae no comments</p>
    </div>
  {{/if}}
</section>